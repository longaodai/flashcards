<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>English FlashCards - Learn Vocabulary Effectively</title>
    <meta
      name="description"
      content="Master English vocabulary with our interactive flashcards. Features pronunciation, progress tracking, and mobile-friendly design. Learn 50+ words effectively."
    />
    <meta
      name="keywords"
      content="English flashcards, vocabulary learning, pronunciation, interactive learning, mobile app, study cards, English words"
    />
    <meta name="author" content="English FlashCards App" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://yoursite.com/" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://yoursite.com/" />
    <meta
      property="og:title"
      content="English FlashCards - Learn Vocabulary Effectively"
    />
    <meta
      property="og:description"
      content="Master English vocabulary with interactive flashcards featuring pronunciation and progress tracking."
    />
    <meta property="og:image" content="https://yoursite.com/og-image.jpg" />
    <meta property="og:site_name" content="English FlashCards" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content="https://yoursite.com/" />
    <meta
      property="twitter:title"
      content="English FlashCards - Learn Vocabulary Effectively"
    />
    <meta
      property="twitter:description"
      content="Master English vocabulary with interactive flashcards featuring pronunciation and progress tracking."
    />
    <meta
      property="twitter:image"
      content="https://yoursite.com/og-image.jpg"
    />

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />

    <!-- Custom Styles -->
    <link rel="stylesheet" href="/css/style.css" />

    <% const cssFiles = typeof css !== 'undefined' ? css : []; %> <%
    cssFiles.forEach(file => { %>
    <link rel="stylesheet" href="<%= file %>" />
    <% }) %>
  </head>
  <body class="min-h-screen flex flex-col">
    <!-- Header -->
    <%- include("../components/header") %>

    <!-- Main Content -->

    <%- body %>

    <!-- Footer -->
    <%- include("../components/footer") %>

    <!-- JavaScript for Theme Toggle and Mobile Nav -->
    <script src="js/theme.js"></script>
    <script src="js/mobile-nav.js"></script>

    <% const jsFiles = typeof js !== 'undefined' ? js : []; %> <%
    jsFiles.forEach(file => { %>
    <script src="<%= file %>"></script>
    <% }) %>
  </body>
</html>
